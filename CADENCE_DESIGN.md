# 온톨로지-유도 갱신 케이던스 — 설계 검토 로그 (Cadence Governance)

> **상태**: 검토 로그(deliberation), 2026-07-27. 확정 설계 아님.
> CORE_ENGINE.md의 O(Δ) 유지엔진 **위에 얹는** "언제·얼마나 자주 갱신하나(시점·주기)"의 온톨로지 거버넌스 방안.
> 미결: 유틸리티 `w_i`·코호트 중복 할인 `r_i`·Tasking 하한의 정확 정의, 솔버 결정, 실험 구현.

## 0. 무엇을 논문값으로 세우려는가
insertion/update를 "알고리즘 한 단락"이 아니라 **프레임워크**로. 단, *무엇을 넣나(내용)* 가 아니라
**언제·얼마나 자주 넣나(시점·주기)** 를 온톨로지가 관장. 초기 걱정 = "결국 잘 정의된 것 넣기 아니냐 / 시점·주기 미고려".

## 1. 왜 "예산 + 경합"이 핵심인가 (그 걱정의 해소)
- 케이던스가 IndexPoint마다 **독립**이면 레짐→주기는 저장 상수의 사영이라 **trivial**.
- **전역 갱신 예산 B**(초당 materialize/write-Tx/연산 상한)가 부족하면 IndexPoint들이 그 예산을 놓고 **경합**한다.
  → "누가 이겨서 자주 갱신되나"를 **온톨로지가 결정** → **주기가 상수가 아니라 경합의 산물(창발)**.
- 코호트 중복 센서 = 경합에서 **짐**(형제가 커버) → 긴 주기. Tasking 구독 = **이김** → 짧은 주기.
- 이로써 "구조가 주기를 정한다"가 비자명해지고, "잘 정의된 것 넣기" 비판을 벗어난다.

## 2. 예산의 근거 = 비용 전제 (정직하게)
- **IoT-J (Observation-Metadata-Centric, 강한 앵커)**: 실험2 = 메타데이터 집계(IndexPoint) **유/무 질의처리시간 비교 → 최대 179.75× 빠름**. = 인덱스 없는 무거운 질의는 극단적으로 느림 → 인덱스가 비용 국소화. **읽기(질의) 측 근거.**
- **OTU (워크숍, 방향 근거만)**: 갱신시간이 n에 초선형(0.08→0.45→3.90s). **워크숍 티어라 하중 인용 부적격.**
- **우리 엔진**: materialize ~≤20ms/slide (CORE_ENGINE §의 실측).
- **정직한 한계**: IoT-J의 179.75×는 *읽기* speedup이지 *쓰기 예산*을 직접 증명하지 않음. 쓰기 처리량 유한성(=예산)은 **유지비용 실험(Inc.4)으로 별도 실측** 필요. overclaim 금지.

## 3. 3요소 골격 (택시웨이의 온톨로지+Jena규칙+OptaPlanner에 대응)
```
온톨로지(구조) → Jena규칙(구조→레짐·유틸리티 유도) → 예산 배분기(솔버) → 유지엔진(O(Δ) 집행)
```
- **온톨로지**: STA 트윈 — MDS·IndexPoint·**공유 metadata 코호트**·TaskingCapability.
- **Jena규칙**: IndexPoint i마다 구조에서 유도 — 코호트 중복 `r_i`, Tasking 신선도 요구 `τ_i`(없으면 ∞), 레짐(HOT/WARM/COLD)→기본 가중 `w_i`. (택시웨이의 "needsNewPlan=true" 대신 "refreshRegime=HOT, utility=w".)
- **배분기(솔버)**: 예산 하 갱신율 배분.
- **유지엔진**: 배분된 주기로 insert/delete를 O(Δ) 집행. 정책층은 **관측 hot path 밖**, 느린 구조-시계에서만 발화(두 시계 분리).

## 4. 최소 형식
```
max  Σ_i  w_i · g(ρ_i)          # g: 오목(신선도 수확체감), ρ_i: IndexPoint i의 갱신율
s.t. Σ_i ρ_i ≤ B                # 전역 갱신 예산
     ρ_i ≥ floor(τ_i)           # Tasking 마감 = 하드 하한
     w_i ↓  as  r_i ↑           # 코호트 중복 → 가중 할인(형제가 커버)
```
오목 → **water-filling/탐욕**: 한계이득 `w_i·g'(ρ_i)=λ` 를 맞춰 배분. 가중 큰(Tasking·저중복) IP가 짧은 주기.
**IP i의 시점·주기 = 1/ρ_i**, 경합의 결과. 구조 변화 시(센서 합류·Task 부착) 가중 변화 → 재배분 → 주기 즉시 이동 = **cold-start 승리**.

## 5. 솔버 선택
- **시작(권장)**: 오목-배분 = knapsack/water-filling → **탐욕 O(k log k)**. 느린 구조-시계에서만. **OptaPlanner 과함.**
- **업그레이드**: Tasking 마감을 하드로 + **갱신 스파이크 회피(phase-staggering)** 를 넣으면 진짜 스케줄링 → OptaPlanner 정당화(+택시웨이 병렬성). phase-staggering은 write-Tx 폭주 방지로 실제 유효.

## 6. 택시웨이(Borjigin & Austin, T-ITS'24)와의 관계
- 그들: 온톨로지+**Jena Rules**+OptaPlanner. 규칙=충돌 검출→`needsNewPlan=true`(트리거). 솔버=경로+이륙시간 재배정. 평가=**정성 PoC(3기)뿐**, 정량평가는 향후과제로 미룸.
- 우리: 조정대상=**유지 케이던스**(그들=플래닝), 트리거=**구조**(그들=이벤트), + 그들이 미룬 **정량실험** 제공. 동일 스택 → **Jena Rules 선택 정당화**(직계 최상위 선례).

## 7. 폐기된 대안 (기록)
- **시간을 개체로 reify하지 말 것**: 비행기 일정을 insert/delete 스케줄로, 시간을 1분 단위 "time-part 개체"로 만들려던 발상 → 너무 저수준·폭발적·운용 불가 → **폐기**.
- **교훈**: 시간은 *개체*가 아니라 **datatype 값 + 유도된 주기 값(period=N초)**. 택시웨이도 Allen 구간(값)으로 처리, 분-개체 안 만듦. 우리 "언제"는 엔진 슬라이드 시계를 그 주기로 변조.

## 8. 절대 지킬 차별선 (자멸 방지)
배분기 자체는 **표준**(knapsack/water-filling). 신규성 = "새 스케줄러"가 아니라 **그 스케줄러의 유틸리티/우선순위가 온톨로지 구조의 엔테일먼트(코호트 중복·Tasking)이고 증분 유지**된다는 것. TII(Prediction-Based Scheduling)와 **같은 knapsack 무대**이되 **유틸리티 출처로 승부**. "새 스케줄러" 주장 시 자멸.

## 9. 이 골격이 여는 실험 (Originality-B)
- 예산 B 고정, **유틸리티만 교체**: (a) 균등, (b) **AoI/긴급성(=TII 베이스라인)**, (c) **온톨로지-구조**. 지표 = 중요상태(Tasking·저중복) staleness 또는 유틸리티-가중 신선도.
- **cold-start**: 우리 유틸리티는 t=0에 구조에서 즉시; AoI/통계 베이스라인은 워밍업 필요 → 구조변화 직후 우리만 옳게 반응.
- 유지비용: 증분 O(Δ) vs 전체 재계산 O(W), flat vs O(m) (Inc.4 연결).

## 10. 미결 (다음 작업)
1. `w_i`(레짐 가중)·`r_i`(코호트 중복 할인)·`floor(τ_i)`(Tasking 하한)의 정확한 정의.
2. 솔버 결정: 탐욕/water-filling vs OptaPlanner(마감+anti-spike 필요 시).
3. 실험 구현: 유지비용(Inc.4) + cold-start + 유틸리티-교체 비교.

## 11. 문제 재검토 — 실측이 "쓰기 비용" 동기를 반증함 (2026-07-27)
**실측(2026-07-27, `/experiment/insert` 하네스)**: 온톨로지를 키워도 삽입시간은 **미세하게만** 증가.
- 균질(occupancy×5): m 0.26M→5.2M(20×)에 관측당 71→104µs (**1.46×**). 이질(23 프로필): m 0.45M→8.9M에 122→150µs (**1.23×**). 대부분 GC 지터, 스캔성 폭증 아님. (자료: `bench_insertion_{seed,hetero}.{json,csv}`, `bench_insertion_RESULTS.md`.)
- = O(Δ) 설계 작동 확인(삽입은 m과 거의 무관). 지속 처리량 ~7.5k~11.8k obs/s → 우리 규모(23 obs/s)는 한참 아래. → **"온톨로지 갱신이 비싸다"는 전제는 실측으로 반증됨. 문제를 쓰기 비용에 세우면 리뷰어가 같은 실험으로 무너뜨림. 삽입 곡선은 오히려 "쓰기비용은 문제 아님"의 반증자료로 사용.**

**진짜 문제는 시스템/스케일에 있다 (현실적, 단위드리프트 같은 작위 배제).** 실측으로 위치 특정:
- **실험1 — 파이프라인 포화**: 센서 23→230→2300 ×1~10Hz, 전부 즉시 materialize. 측정=end-to-end 지연·처리량·드롭. 단건 삽입 싸도 *전체 파이프라인(수신→파싱→resolve→writeTx→materialize→2차융합)* 이 총률에서 포화 → **예산은 파이프라인 처리율에 실재**. ("실시간 반영 자체가 문제".)
- **실험2 — 쓰기-읽기 경합**: 소비자 SPARQL 질의 지속 + materialize 빈도↑ → 질의 지연↑(Jena writeTx가 readTx 차단). over-update의 진짜 대가 = **질의 응답성**. IoT-J 질의축(179.75×) 연결.
- **실험3 — 무한 성장/삭제 필요**: 삭제無 vs 윈도우 삭제, 상주 트리플·메모리·질의지연 시간변화. 보존길이 = 스트림별 governable 노브(중복=짧게, Tasking=길게). (Inc.4 절반 완료.)
- **실험4 (성과) — 예산 하 배분**: 실험1·2의 지속가능 예산 고정, 정책 교체 균등 vs AoI(TII) vs 온톨로지-구조. 현실 비정상성=**센서 합류/이탈/고장**(단위드리프트 아님). 측정=중요상태(Tasking·저중복) staleness + 질의 응답성. + cold-start.

**정직한 관문**: (1) **실험1이 토대** — 예산 실재 여부(우리 규모선 포화 안 남 → 합성 스케일업 필수). 안 나면 정확성/질의경합으로 무게이동. (2) **병목 귀속 주의** — 브로커/네트워크 자체가 병목이면 우리 기여 아님(응용논문 위험). 예산은 **우리가 통제하는 트윈-측(materialize+2차융합+writeTx+메모리)** 에 걸어야 함.

**재프레이밍**: "갱신이 비싸다"(약함) → "**중요상태를 낡거나 틀리게 두지 않고, 중복상태에 낭비 안 하며, 그 판단을 온톨로지 구조로 한다**"(강함). 예산 = compute 상한이 아니라 *지속가능 materialize율(실험1·2)* 또는 *staleness 예산*.

**권고 순서**: 실험1 → 실험2 먼저(예산 실재 확립) → 실험4(성과). w_i/r_i 정의는 그 다음(어떤 문제를 푸는지 확정 후).

## 12. 정확성 문제 (비용 아닌 "틀림") — 2026-08-03
삽입비용 반증 후 남은 강한 문제. 트윈이 **조용히 틀린 융합값을 서비스**하는 것(비정상성 하). 시나리오:
1. 센서 고착(freeze/dropout) → 코호트 평균 오염, 윈도우 만료로도 안 사라짐(계속 같은 값 송신).
2. 재배치/멤버십 드리프트 → 엣지 재라우팅 안 하면 다른 물리량이 엉뚱한 코호트로 유입.
3. 펌웨어 스케일 변경(µg↔mg, W↔kW) → 값이 자릿수 단위로 오염(현실적, °C/°F 같은 작위 아님).
4. 전이 뭉갬 → 너무 긴 윈도우가 스파이크를 평균으로 삭제 → 트윈이 "정상"이라 보고.

**★ 시나리오 나열 ≠ 기여 (경고)**: 1~4를 나열만 하면 서술/응용 논문(MDPI 함정). 기여로 올리려면 —
(a) **하나의 문제로 형식화**(비정상 파티션 정확성 불변식+오차 정의), (b) **하나의 메커니즘**(멤버십 엣지 insert/delete/reroute + 윈도우 distributive 집계 = 모두 같은 대수), (c) **구조적 보장**: 코호트 |C|에서 단일고장 융합 영향 ≤ **1/|C|**(중복=구조적 오차경계), 정정지연 ≤ 구조-유도 재검증주기 → 오차×지연 균일 한정, 복원 O(Δ). (d) 시나리오는 *평가 배터리*로 강등. **(c)가 있으면 기여, 없으면 서술.**
- 정직 경계: 고착 "탐지" 자체는 통계(outlier); 온톨로지 몫 = 비교집단(코호트) 정의 + 대응(reroute) + 케이던스(중복·Tasking). 탐지는 부품.

## 13. 중심주제 이동: STA Tasking (지각–행동 루프) — 2026-08-03 [제안]
**동기**: 지금까지 "큰 메인 주제" 부재 + 단위/코호트 의존 아쉬움. → 중심을 **실시간 삽입(지각) + 실시간 제어(STA Tasking, 행동)의 조율**로 이동.

**통합 테제**: *스트리밍 하 의미론적 트윈을 SensorThings Tasking에 충분히 신선·정확하게 유지 — 제어 요구가 삽입/재물질화/보존 케이던스를 지배하고, 비정상성 하 오염이 잘못된 액추에이션을 유발한다.*

**왜 강한가**: (1) 없던 메인 주제(perception–action loop) 확보. (2) 정확성이 *consequential*(틀린 상태→틀린 액추에이션=안전훅, 택시웨이가 T-ITS 간 방식). (3) 케이던스 근원이 units/cohort가 아니라 **제어 신선도 요구(TaskingCapability)** = 단위 무관. (4) 실제 OWL 어휘(STA Tasking 준비됨), background 검증(QoC 코디자인 TII, Age-of-Task). (5) 이전 실타래(삽입·케이던스·정확성·보존) 흡수.

**요구조건·리스크**: (a) STA Tasking엔 **Status/State 어휘 ABSENT** → 엔테일 유도 또는 가산 명시. (b) 실제 액추에이션 아닌 **모델링/실험**("틀린/늦은 Task 발행" 측정). (c) **제어이론으로 새면 스코프밖(TAC/TCST)** → 의미론/시스템 조율로 TII/IoT-J 톤 유지. (d) **"STA Tasking + 실시간 트윈 유지" 신규성 타깃 확인 필요.**

**미결**: 신규성 확인 → 어떤 "문제 발생"을 헤드라인으로(틀린 액추에이션 vs 케이던스 미스매치 vs 경합) → 실험 설계. (→ §14에서 해소)

## 14. 신규성 판정 + 헤드라인 확정 + 판별 실험 (2026-08-03)

### 14.1 신규성 3축 조사 결과 — **3축 모두 OPEN** (상세: references.md §G)
- **축 A (STA Tasking 사용 연구)**: OPEN. `TaskingCapability` 전문검색 전 세계 8건, Tasking+RDF=0, Tasking+신선도=0. 유일 실배치(Paulau ISPRS'24)는 구문적·크론규칙·무평가.
- **축 B (액추에이션 온톨로지 × 케이던스)**: OPEN. 분야 서베이(Esnaola-Gonzalez SWJ'20)에 신선도/케이던스 차원 자체가 부재. Status/State 어휘: 어떤 표준에도 명령 수명주기 없음 → IoT-Lifecycle 패턴+FIWARE `_status` 관례 근거로 **가산**(§13(a) 해소).
- **축 C (freshness-for-control × 의미론)**: OPEN-narrow. **TRW(Chang, arXiv 2026-07)가 최대 위협** — 결과-스케일 신선도 마감이 refresh를 지배(정리 포함), 단 온톨로지 부재(per-claim 수동 선언). QoC 문헌은 신선도→케이던스 역전파 부재 확정.
- **★ 생존 조건(TRW 대비)**: 기여 문장을 "신선도 요구→케이던스"(선점됨)가 아니라 **"온톨로지 구조가 케이던스를 유도한다 — Tasking 부착·코호트 중복의 엔테일에서, per-claim 수동 선언 없이"** 로. §8(유틸리티 출처로 승부)·F절 driver-substitution과 일치.

### 14.2 헤드라인 "문제 발생" 확정 — **틀리거나 늦은 Task 발행 (wrong/late actuation)**
세 후보는 경쟁이 아니라 **한 인과사슬의 층위**로 판명:
```
예산 실재(경합·무한성장: 실험2·3, 실측) → 케이던스 배분(구조-유도 정책: 메커니즘)
  → 틀리거나 늦은 Task 발행(결과 = 헤드라인 지표)
```
- **경합 단독**: Jena 스토어 아티팩트("RDFox 쓰면 되잖나" 반론) → 헤드라인 부적격, **예산 실현자**로 강등. 단 Tasking 렌즈로 결과화: 컨트롤러의 결정-읽기(SPARQL)가 경합에 밀리면 → **늦은 Task** (IoT-J 질의축 연결).
- **케이던스 미스매치 단독**: 내부 지표, 결과 없음 → 서술적. 메커니즘 층으로.
- **틀린 액추에이션 단독**: consequential(T-ITS 교훈: 안전 훅)이나 TRW 인접 + "탐지=통계" 함정 → **§12(c) 구조 보장과 결합해야 기여**: 문턱 결정함수 f(마진 δ) 하에서 단일고장 편차 Δmax의 융합 영향 ≤ Δmax/|C| → **wrong-task는 Δmax/|C| > δ일 때만** = 코호트 크기가 구조적 안전마진. 정정지연 ≤ 구조-유도 재검증주기 → 노출창 한정. (동역학·안정성 없음 = 제어이론 스코프밖 유지, §13(c) 준수.)
- 헤드라인 지표: **wrong-task rate / missed-task rate / task lag**(오라클 대비), 보조: staleness-at-decision-read, 결정-읽기 p99.

### 14.3 판별 실험 설계 (모델링된 Tasking, §13(b) 준수 — 실제 액추에이터 없음)
공통 하네스: 시뮬레이션 센서 레인(23 프로필) + **접지 진실(ground truth)** + §12 비정상성 배터리(고착·이탈·멤버십 드리프트·스케일 변경 = 평가 배터리로 강등, §12(d)). 트윈은 STA Part 2 형태의 TaskingCapability + 가산 어휘(신선도 요구 τ, 수명주기 상태) 선언. **컨트롤러 모델 = 트윈 융합상태 위 결정적 문턱 정책**(예: 점유/온도 문턱 초과 → 파라미터 포함 Task 발행). **오라클 = 동일 정책을 접지 진실에 적용** → 정답 Task 스트림. 지표 = 발행 Task 스트림 diff(wrong/missed/lag).
- **E-T1 정책 교체 (성과 주장)**: 지속가능 예산 B 고정(실험1·2에서 실측), 케이던스 정책만 교체 — 균등 / 주기 / AoI·긴급성(TII Kim 베이스라인) / **구조-유도(우리)**. 측정: wrong/late-task + 중요상태 staleness. 기대: 같은 예산에서 우리 정책이 Tasking-부착·저중복 상태를 우선해 wrong/late 최소.
- **E-T2 cold-start (구동자 치환의 실효 증명)**: 런타임에 TaskingCapability 부착/탈착, 코호트 합류/이탈 → 재배분 지연 + 변화 직후 창의 wrong/late. 통계/AoI 정책은 워밍업 전 원리적 무반응 — F절 판별 실험의 Tasking 판.
- **E-T3 구조 보장 검증 (§12(c) 실증)**: 센서 1개 고착, |C| ∈ {1,2,5,10,23} 스윕 → 융합오차 vs 1/|C| 예측 일치 + wrong-task 발생 문턱(Δmax/|C| vs δ) 확인.
- **E-T4 경합→행동 결과화**: materialize율 스윕 → 결정-읽기 지연 → task lag 전이 곡선. "over-update의 대가"를 질의응답성이 아니라 **행동 지연**으로 표현(실험2 흡수).

### 14.4 리뷰어 선제 방어 목록 (G절에서 승격)
1. "왜 Tasking 연구 채택이 없나" → Tasking=제어행동의 기계가독 선언 = 케이던스 정책의 필요 입력(약점→기여 전환).
2. `ssn-system:Frequency` → 정적 능력 선언 ≠ 런타임 신선도 정책.
3. Seydoux IoT-O → 방향 반대(온톨로지→합법행동, ≠ 온톨로지→유지 케이던스).
4. TRW → 구조-유도 vs per-claim 수동 선언 (14.1).
5. CoaaS → 비의미론 캐시+SLA 비용 vs RDF+액추에이션 정확성.
6. World Avatar DIF → 공급측 push vs 수요측 pull (⚠️ 전문 확인 최우선).
7. "semantic"=시맨틱 통신 이름충돌 (F절 기존 경고 유지).
